# Seven toys in a child's room into a bookcase: two dice and one board game on the bed, one board game and the
# train set on the desk, two teddies on the floor. The goal names two bookcases (one instance plus a wildcard,
# both inroom childs_room) but wraps every item in one "exists a bookcase", so all seven must end up inside the
# SAME one -- unlike putting_away_toys, where each toy may go to whichever box is nearest.
# For an 8 cm jaw: the two board games are flat wide boxes, grippable only by pinching a rim or a corner, never
# across the lid; the train set is a bulky multi-piece set; the teddies are soft and wider than the jaw at the
# body, so they take a limb or an ear; the dice are small enough to fall inside the jaw but start on a bed, a
# soft surface a nudge moves. Placing is into a shelf entered from the side, as in re_shelving_library_books,
# not dropped into an open bin.
task: collecting_childrens_toys
# The instruction is the task string the planner is given (bench.py: args.task = spec.instruction), so it has
# to name the things that are actually there. "the toys" named nothing in this definition -- the items are
# dice, teddies, board games and a train set, and a die is not found by looking for a toy -- which left four
# of the seven unnamed to the planner. "the same bookcase" is in there because the goal's exists makes the
# two bookcases interchangeable, and seven items split between them score zero.
instruction: put the dice, the teddy bears, the board games and the train set in the same bookcase
plan: transfer
