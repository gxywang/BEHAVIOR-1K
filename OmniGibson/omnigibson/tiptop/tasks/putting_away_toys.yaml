# Eight toys off two floors into either toy box (one stands on the floor, one on a table): the goal is
# "forall toy, EXISTS a toy box", so any box takes any toy and the boxes are interchangeable.
# The runner filled whichever was nearest, which sent the dining-room toys to toy_box_2 on the table. Measured
# over three runs (0915_045032, 0915_082053, 0915_124450), rounds aimed at each box:
#   toy_box.n.01_1  (bottom z 0.00, on the floor)   17 of 23 executed
#   toy_box.n.01_2  (on a table)                     0 of  7 executed  -- not one, ever
# Naming the floor box collapses the demand onto the reachable one. It can only narrow the demand: every toy in
# toy_box_1 satisfies the goal outright, so nothing is given up (2026-09-15).
task: putting_away_toys
instruction: put the toys in the toy boxes
plan: transfer
container: toy_box.n.01_1  # the table-top box has never had a single round execute
