# Eight toys off two floors into either toy box (one stands on the floor, one on a table): the goal is
# "forall toy, EXISTS a toy box", so any box takes any toy.
#
# A `container: toy_box.n.01_1` was tried on 2026-09-15 and WITHDRAWN the same evening. The reasoning was that
# rounds aimed at the floor box executed 17 of 23 while the table-top box managed 0 of 7, so naming the floor box
# could only narrow the demand. That is true of the GOAL and false of the GEOMETRY: the box is 0.62 x 0.88 m with
# its rim at z 0.46, and eight toys do not fit. The run that committed to it placed all 8 with zero planning
# failures and still left 3 atoms open -- toy_figure_5, _7 and _8 all came to rest with their bottoms at z 0.46,
# exactly the rim, sitting on the pile rather than in the box. 0.625 against 0.875 for the nearest-box routing.
#
# The two boxes exist so the toys can be SPLIT. What would actually help is filling the reachable box until it is
# full and then using the other, which is a skill the runner does not have.
task: putting_away_toys
instruction: put the toys in the toy boxes
plan: transfer
